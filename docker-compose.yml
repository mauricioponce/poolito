version: '2'

services:
  nice-pool:
    container_name: nice-pool
    build:
      context: .
      dockerfile: Dockerfile
    command:
      - '/bin/bash'
    tty: true

  nice-redis:
    container_name: redis-pool
    image: redis:latest
